[project]
name = "voice-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
# The agent itself lives in carrier_voice_agent/ and declares its own
# dependencies. This outer project is `uv init` scaffolding and needs none.
dependencies = []
