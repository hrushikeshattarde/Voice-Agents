.env
.venv/
__pycache__/
*.py[cod]
carrier_agent.db
carrier_agent.db-journal
# Real reps' names and phone numbers — local deployment data, not repository
# content. `reps.toml.example` is the template.
reps.toml

# Audio written by tools/audition_voices.py when comparing TTS voices.
voice-samples/

# Runtime audio: practice-session clips/recordings and phone-call recordings.
# These are recordings of real people — local data, never repository content.
practice_audio/
call_recordings/

# build / packaging
build/
dist/
*.egg-info/

# test / tooling caches
.pytest_cache/
.ruff_cache/
